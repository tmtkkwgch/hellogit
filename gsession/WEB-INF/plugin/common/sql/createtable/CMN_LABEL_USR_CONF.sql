create table CMN_LABEL_USR_CONF
(
     LUF_EDIT        integer        not null,
     LUF_SET         integer        not null,
     LUF_AUID        integer        not null,
     LUF_ADATE       timestamp      not null,
     LUF_EUID        integer        not null,
     LUF_EDATE       timestamp      not null
) ;
